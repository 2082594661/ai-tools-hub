# 2026-07-25 工作记录

## 战略决策（用户授权"全交给你决定"）
- 文章降级为 SEO 漏斗页：保留、不新写、加 CTA 导工具（用户认为文章无用、没人看）。
- 小工具升级为站核心：用户认可"小工具可以考虑"。

## 本轮交付
- 新建 `tools.html`（工具 Hub 页，含 SEO/JSON-LD/BreadcrumbList）。
- 新建 `assets/js/tools.js`：3 个纯前端零 API 工具（爆款标题生成器 / 种草文案生成器 / 图片智能压缩 canvas）。
- `style.css` 追加 `.nav-cta`（全站 header 醒目 CTA + pulse 动效）与 `.cta-tools-band`（文章页底部导流大区块），CSS 184→200 括号平衡。
- 批量注入：41 页加 nav CTA（tools.html 自身跳过），15 篇文章页加底部 CTA 区块。
- 踩坑：Windows glob 返回反斜杠导致子目录 `../` 前缀丢失 → 修复 13 个子目录页 nav-cta 路径为 `../tools.html`。
- 校验：静态审计 42 文件 0 问题；JS node --check 通过；CSS 200/200。

## 待用户拍板
- 真实 ICP 备案号、确认域名 aifunnyplay.cn、广告代码（同前）。
- 是否要把小工具升级为"真 AI 生成"（需接 API + serverless，有算力成本）。

## SEO + PWA 增强 + 上线前终审（本轮）
- PWA：`manifest.json` + `sw.js`（cache-first 静态资源 / navigation network-first）+ `assets/js/site.js` 内注册（仅安全上下文）。
- 用 PIL 生成 4 张图标：icon-192/512 + icon-maskable-192/512（#667eea 底 + AI 字），供 manifest 与 apple-touch-icon。
- index.html / tools.html / 6 篇 how-to 文章 加 `<link rel=manifest>` + apple-touch-icon + apple-mobile-web-app-* 标签。
- `tools.html` 加 FAQPage JSON-LD（5 组问答，已 json.loads 校验通过）。
- 补齐 3 个缺失标签页：`tag/ai-video-audio.html` / `tag/ai-office.html` / `tag/ai-writing.html`（用脚本按真实文章集生成，新文章用渐变 banner，老文章用 webp）。
- `sitemap.xml` / `sitemap.html` 补登 tools.html + 6 文章 + 3 标签页。
- 静态审计（audit_static_site.py）结果：真实断链 6→0；剩余 19 条 `tools.html#锚点` 为审计脚本不剥离 URL fragment 的误报（已逐一确认 #qr-gen/#tts/#stt/#art-text/#title-gen/#copy-gen/#img-compress 在 tools.html 均存在）。
- 全站 51 个 HTML 标签闭合平衡；CSS 214/214；JS node --check 全过；sitemap.xml XML 解析通过。
- 本地 `python -m http.server 9999` 起服务，首页/tools/标签页/资源均 200。
- 部署待办：真实 ICP 号（footer 占位 `粤ICP备XXXXXXXX号-1`）、域名确认、广告代码粘贴进 4 个 `.ad-slot`。

## 用户反馈处理（2026-07-25 晚）
- 用户要求删除 tools.html 侧边栏两个 FAQ widget："为什么做这些小工具" + "想要更多工具？" → 已删除，连同空 `<aside class="sidebar-col">` 一并移除，标签平衡校验通过。
- 用户要求删除全站顶部导航栏"关于本站"+"友情链接"两个链接 → 用 Python re.sub 批量处理 51 个 HTML 文件（含根目录+tag/+category 子目录），仅删 header nav 内的两行，footer 保持不变。部署端口改为 8088。

## 工具精简 + 事故恢复（2026-07-25 深夜）
- 用户决策：文章方向不确定先停更；**砍掉 4 个弱工具**（文字转语音 TTS、语音转文字 STT、爆款标题生成、种草文案），只保留 3 个纯前端稳工具：二维码生成器、图片智能压缩、艺术字生成器。
- ⚠️ 事故：用正则删 tools.html 的 4 个 section 时，因 `<!--` 贪婪匹配误删整页中部（JSON-LD/style/6 个 section），tools.html 只剩 92 行。文件未进 git、无备份。已用本会话早前逐行读过的原始内容**完整重建** tools.html（3 工具 + FAQ 去 TTS 条 + 更新 title/desc/keywords）。教训：删 HTML 大块用 Python 按 id 精确删除时，别让 `<!--` 模式跨块贪婪吞掉保留区。
- 配套清理：tools.js 删 initTTS/initSTT/genTitles/genCopy 等 4 函数+初始化（node --check 通过）；index.html 删 4 张工具卡、数量改"3 个"、去残留工具名；4 篇 how-to 文章 CTA 锚点由 #tts/#stt/#title-gen/#copy-gen 改为 tools.html，正文"用本站的X工具"改中性。
- 最终核验：真实断链 0；tools.html 标签平衡、3 段 JSON-LD 解析通过；全站 51 文件无残留已删工具引用。静态审计 7 条"断链"均为审计脚本不剥离 #锚点的误报（指向保留的 qr-gen/art-text/img-compress，锚点真实存在）。
- 本地服务仍在 8088（python -m http.server 8088）。

## 首页改工具箱 + 文章全删（2026-07-25 深夜）
- 用户决策：首页直接改成工具箱（不再导流到 tools.html）；现有文章"完全不行"全删。
- 执行：index.html 重写为工具箱（3 工具直接内嵌 + FAQ 结构化数据），删工具列表/侧边栏文章导航/footer 文章分类；原 tools.html 合并后软删到项目外可恢复备份 `C:\Users\龙潜\Desktop\AI趣味分享_已删文章备份_2026-07-25\`（41 篇文章/页 + tools.html，按 articles/tag/category 分好类）。
- 批量软删 42 个文件：根目录 26 篇 + tag/ 10 + category/ 6；清空 tag/、category/、_posts/ 目录；6 个系统页 footer 的"文章分类"列断链改为指向系统页；404 页 category 死链改系统页；sitemap.xml/sitemap.html 重写为只剩 8 个系统页。
- 核验：真实断链 0（自定义全站扫描 + 静态审计双重确认）；审计仅剩 3 条 sitemap.html→index.html#qr-gen 之类锚点误报；站点从 51 个 HTML 精简到 8 个（404/about/contact/disclaimer/index/links/privacy/sitemap）。

## 导航栏"联系我"删除（2026-07-25 22:40 左右）
- 用户："联系我也删除"。执行：用 `(?<!<li>)` 负向后顾只删 `<nav>` 块内的裸 `<a href="contact.html">联系我</a>`，保留 footer（`<li>` 包裹）和 contact.html 页面本身（类比之前"关于本站/友情链接"只删 nav）。
- 核验：全站 8 个文件 nav 均确认无"联系我"，首页/小工具按钮保留；footer 仍保留联系我入口。若用户要彻底删 contact.html 页面+footer 链接，需走可恢复备份并先确认。
