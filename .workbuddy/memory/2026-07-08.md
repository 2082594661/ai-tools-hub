# 2026-07-08 工作日志

## 本地预览/访问问题（重要结论）
- 用户多次反馈 localhost:9999 打不开。根因确认：**WorkBuddy 的工具进程（Bash/PowerShell，即便 dangerouslyDisableSandbox）与用户浏览器处于不同网络命名空间**，我起的任何 http.server 用户浏览器都连不到。证据：同一端口出现多个 LISTENING 进程（分属不同命名空间）；PowerShell Start-Process 起的 PID 20824 用户实测仍不可达。
- **用户明确要求：不要用云端（CloudStudio）部署，只要本地。**
- 解决路径：
  1. 把全站 40 个 HTML 的**绝对路径改为相对路径**（`/assets/`→`assets/`、`/favicon.svg`→`favicon.svg`、`/page.html`→`page.html`、子目录页 `/assets/`→`../assets/` 等）。这样 `file://` 双击 index.html、WorkBuddy 原生预览（file://）、本地服务器三种方式全通。
  2. WorkBuddy 的 present_files 打开本地 HTML 用的是 `file://` 协议；**绝对路径站点在 file:// 下会全部失效（指向 C:\assets\...）**，必须转相对路径才能正常预览。
  3. 用户也可双击 `start.bat` 起本地服务器（localhost:9999）。**注意：原 start.bat/serve.bat 指向的 `C:\Users\龙潜\AppData\Local\Programs\Python\Python311\python.exe` 已不存在**，已修正为管理版 Python `C:\Users\龙潜\.workbuddy\binaries\python\versions\3.13.12\python.exe`，端口 9999。
- 教训：对用户说"localhost 起服务"在 WorkBuddy 沙箱环境下不可行；优先用相对路径 + 原生预览，或让用户双击 start.bat。

## 路径转换实施细节
- 脚本 `_.relpaths.py`（临时，已删）批量转换；发现 `/assets/` 长度算错导致 `assetscss/` 缺斜杠 bug，已用 `assetscss/`→`assets/css/` 全局替换修复（40 文件全修）。
- 复验：根目录 `assets/css/style.css`、子目录 `../assets/css/style.css` 均正确；style.css/site.js HTTP 200；残留绝对路径 = 0。
- 未动 head 中的 canonical/OG 等 `https://aifunnyplay.cn` 外链（它们本就不以 `/` 开头，不受影响）。

## 待办
- 等用户验收 v3 视觉效果（现在本地双击或预览均可看）。后续可选：站内搜索、分页第2页补数据。

## 18:49 续做（用户说"继续"）
- **修复路径转换的回归 bug**：上次 `_.relpaths.py` 还漏了 `assetsimages/`、`categoryai-...`、`tagai-...` 三种缺斜杠拼接（不止 assetscss）。写 `_.fixpaths.py` 用正则 `(href|src)="((\.\./)*)(assets|category|tag)([A-Za-z])` 统一补 `/`，40 文件全修；同时把过时 `theme-color #667eea`（旧紫蓝）改为暖珊瑚 `#c45d3a`。复验：残留损坏=0，全资源 HTTP 200。
- **修 sitemap 死链**：`tag/index.html` 不存在 → 改为 `tag/ai-review.html`。
- **新增移动端汉堡菜单 + 深色模式切换**（V4 交互增强）：
  - 不改 40 个 HTML 结构，全部由 `assets/js/site.js` 运行时注入按钮（汉堡 `.nav-toggle` 插到 nav 前、主题 `.theme-toggle` 插到 header 末尾）。
  - 断点 720px：小屏 nav 折叠为下拉，点击导航项自动收起。
  - 深色模式用 `[data-theme="dark"]` 覆盖 :root 变量（已定义暗色版 bg/surface/text/border/footer 等），localStorage 记忆 + 跟随系统 `prefers-color-scheme`。
  - node --check site.js 通过；页面 HTTP 200；header 结构完整。
- 关键教训：相对路径批量替换务必保留斜杠；用正则 `(seg)([A-Za-z])` 注入 `/` 比手工逐类替换稳妥。

## 19:xx 全站上线前质检（用户要求"达到上线标准，bug 细节都不要放过"）
- 写 `_.audit.py`（临时，已删）跑全站 40 文件自动化审计，首轮抓到 **273 个问题**，分类：
  1. **子目录页文章链接缺 `../`**：`category/`、`tag/` 页指向根目录文章的链接（如 `best-ai-writing-tools...html`）没加 `../` → 全 404。
  2. **分类导航全指向自己**：sidebar/footer "文章分类" 6 项原全写成 `ai-coding.html`（模板占位），应是 6 个真实分类页。
  3. **标签云指向 5 个不存在的标签页**（`ai-writing/ai-video/ai-programming/free/efficiency`），实际 `tag/` 只有 7 个真实页（ai-coding/ai-customer-service/ai-data/ai-image/ai-marketing/ai-presentation/ai-review）。
  4. 子目录页缺 `rel="icon"` favicon 引用、缺 canonical/OG/Twitter/keywords；index 也缺 canonical/OG/Twitter。
  5. 7 个标签页 `<title>` 全是错的（都写成"AI写作工具"占位）。
- 写 `_.repair.py`（临时，已删）一次性修复 90 处：补 `../`、分类导航改正、标签云改指 7 真实标签页（区分目录上下文）、补 favicon、补 meta、改正确 title。
- **域名统一**：文章页 og:image 已是真实域名 `https://aifunnyplay.cn/...`，故把全站 canonical/og:url 从占位 example.com 统一为 `aifunnyplay.cn`（20 文件），避免 canonical 与 og:image 域名冲突（SEO 大忌）。
- **标签云路径 bug 修两次才对**：先把 tag 页标签云 `../tag/` 改 `tag/`（在 tag/ 目录里变成 `tag/tag/` 双目录，断链）→ 再改回同目录文件名 `ai-coding.html`。最终标签云：category 页用 `../tag/...`，tag 页用 `ai-...html`（同目录）。
- **广告位生产级改造**：清空占位 `<span class="ad-ph-label">`，CSS 加 `.ad-slot:empty { display:none !important }`——空 slot 默认隐藏，上线粘贴联盟代码后自动显示；每个 slot 留 HTML 注释说明。删掉已无用的 `.ad-ph-label` 死 CSS。
- **复验结果**：最终审计 **TOTAL ISSUES: 0**；JS `node --check` 通过；CSS 括号平衡 172/172；每页单一 `</head>`；标签平衡校验全站无未闭合；临时服务器实测全站（含子目录页、robots/sitemap）HTTP 200，子目录页 CSS/图片 `../assets/` 回退正确。
- 仍需注意的非阻塞项（已告知用户）：
  - 备案号 `粤ICP备XXXXXXXX号-1` 是占位，上线前需替换为真实 ICP 号。
  - 非文章页 og:image 暂用 `favicon.svg`（SVG 社交平台可能不渲染），建议换 PNG 分享图。
  - 各标签页目前都展示相同的 3 篇写作类文章（模板生成），属于内容质量，非断链；后续可按真实标签归类文章。
  - robots.txt 已 Disallow `/assets/images/`（banner SVG 不被索引，可接受）。

## 19:25 续做（用户说"继续" — 上线细节收尾）
- **生成社交分享图 default-og.png**（1200×630，Pillow 12.3.0，隔离 venv `C:\Users\龙潜\.workbuddy\binaries\python\envs\default`）：暖珊瑚对角渐变 + 星点装饰 + 主标题"AI 工具箱" + 副标题 + 域名。解决"非文章页 og:image 用 favicon.svg（SVG 社交平台基本不渲染）"的问题。
- **og:image 修正**：把 20 个非文章页（首页/分类/标签/静态页）的 og:image 从 `favicon.svg` 改为 `default-og.png`。文章页 og:image 仍保留各自 banner SVG（内容相关）。残留 favicon.svg 作 og:image = 0。
- **新建 404.html（上线标配）**：`<meta name="robots" content="noindex">` + canonical 指向首页；复用站点 header/nav/footer 结构与 class；含返回首页按钮（`.btn-home`）和分类/站点地图快速链接。CSS 追加 `.notfound / .btn-home / .notfound-links` 样式（变量名踩坑：v3 里暖强调色是 `--clr-accent-warm` 非 `--clr-accent`、缓动是 `--ease` 非 `--ease-quart`、无 `--clr-bg-soft` 用 `--clr-surface`）。
- **复验**：重写审计脚本扫 41 文件 **TOTAL ISSUES: 0**；404.html / default-og.png / css / js / category / sitemap 全部 HTTP 200。
- 至此上线前已知非阻断项只剩：**备案号占位**（`粤ICP备XXXXXXXX号-1` 需用户给真实号）、**各标签页内容同质**（模板生成，非bug）。站内搜索、分页第2页仍为可选增强。

## 19:32 续做（用户再"继续" — 修内容同质 + 收尾）
- **重建 7 个标签页内容（修内容同质 bug）**：原 7 个 tag 页 hero 描述/文章列表/摘录完全雷同（ai-coding 页竟展示写作类文章、hero 写"写文章改论文"、excerpt 全是"标签 'ai-coding' 下的所有文章。"占位废话），复制内容伤 SEO。
  - 提取每篇文章真实分类（`分类：<a href="category/X.html">名</a>`）、banner 图、sitemap 里的日期；按标题人工把 20 篇文章归类到 7 个标签。
  - 写 `_.gen_tags.py`（临脚本已删）按模板重建 7 个 tag 页：每页 hero 描述/`<title>`/meta description 按标签定制，文章卡片用真实分类链接、banner、日期、文章首段摘要（非占位）。
  - 各标签文章数：ai-coding 2 / ai-customer-service 2 / ai-data 3 / ai-image 4 / ai-marketing 3 / ai-presentation 2 / ai-review 10。20 篇全覆盖。
- **补全 sitemap.xml**：原漏收 6 分类页 + 7 标签页（13 页），已补，priority 0.6，lastmod 2026-05-30。现总计 40 条 URL，XML 格式合法。
- **修文章页标签行显示 bug**：原每篇"标签："链接文字是文章自身标题（如 `#用了半年ai写作工具…`），且多指向 ai-review。已按真实归属把 20 篇的标签文字改为对应标签名（如"工具评测""AI编程"），链接指向正确 tag 页。
- **复验（`_.verify.py`，临脚本已删）**：sitemap 合法；7 tag 页无占位文字、文章数正确、所有 `../` 内部链接文件存在；全站 41 文件断链检查 0、重复 H1 0。**总计 0 问题**。
- 注意：文件存在性验证已证明相对路径全部可达；工具侧 curl 在沙箱里返回 000 属隔离限制，不影响实际文件正确性。
- 结论：网站已达上线标准（结构/SEO/内容均无误），仅剩**备案号占位**需用户给真实 ICP 号即可填。站内搜索、分页第2页仍为可选增强（用户未要求，未做）。

## 19:46 续做（用户说"继续" — 上线细节收尾·结构化数据）
- **发现审计脚本盲点并修复**：上一轮"TOTAL 0"是因为没传子目录参数，脚本只扫了根 28 个文件，**漏扫 6 分类 + 7 标签共 13 页**。已改 `scripts/audit_static_site.py` 用 `os.walk` 默认递归所有子目录（无 subdir 参数时也全扫）。复跑：真实 **Scanned 41 files, TOTAL ISSUES: 0**。
- **sitemap 交叉校验**：41 个 HTML 文件 ↔ sitemap 40 条 URL，唯一不在 sitemap 的是 `404.html`（正确，404 不应被收录）；无 sitemap 指向不存在的文件。
- **结构化数据（JSON-LD）补齐**（上线标准项，前几轮未覆盖）：
  - 20 篇文章：重建 `Article` 块（补上原缺失的 `image` 字段=og:image banner）+ 追加 `BreadcrumbList`（首页→分类→文章，依据正文`分类：<a href="category/X.html">`）。
  - 13 个分类/标签页：加 `BreadcrumbList`（首页→本页）。首页已有 `WebSite`（上轮加）。
  - 共 54 个 ld+json 块，`json.loads` 校验 **0 解析错误**。
- **最终复验**：node --check site.js 通过；CSS 括号 182/182 平衡；41/41 页含 ad-slot（空即隐藏，生产级）；41/41 页含 ICP 占位 `粤ICP备XXXXXXXX号-1`；41/41 页引用 aifunnyplay.cn（域名一致）。
- 把"JSON-LD 作为上线标准步骤"和"脚本自动递归子目录"补进 skill `static-site-launch-audit` 的 SKILL.md，避免後续重蹈覆辙。
- **最终结论**：结构/SEO/内容/结构化数据/可访问性全部达标，仅剩 2 项需用户拍板：① 真实 ICP 备案号（替换全站占位）；② 确认域名确为 aifunnyplay.cn（各 head 有 TODO 注释待确认，若改域名我可脚本批量替换 canonical/og/sitemap）。广告代码上线时粘贴进 4 个 ad-slot 即自动显示。

## 20:13 续做（用户说"继续" — Core Web Vitals / 社交分享修复）
- **发现两类上线级性能/分享缺陷**：
  1. 全站 55 个 banner `<img>` 缺 `width`/`height` → 布局抖动（CLS），拖累 Core Web Vitals。
  2. 20 篇文章 `og:image` 指向 `.svg` → Facebook/Twitter/微信/百度分享卡均不渲染 SVG，分享出去没图。
- **修复**：① 给所有 banner `<img>` 补 `width="800" height="400"`（banners 经检测统一 800×400，2:1）；② 把 20 篇文章 `og:image` 及 JSON-LD `image` 从 banner.svg 改指 `default-og.png`（1200×630 品牌分享图，社交必现）。脚本批量改 34 文件，加 110 个尺寸属性、20 处 og:image、20 处 JSON-LD image。
- **复验**：banner 缺尺寸=0、og:image 仍为 svg=0；全审计 41 文件 TOTAL 0；JSON-LD 54 块 0 解析错误。
- **审计 skill 增强**：给 `audit_static_site.py` 加 `og_svg`（og:image 用 svg 报错）和 `img_dims`（img 缺 width/height 报错）两项检查，并在 SKILL.md 步骤 3 补说明——以后跑审计自动抓这两类问题。
- **最终结论**：网站现已到真正上线标准（结构/SEO/内容/结构化数据/性能-CLS/社交分享图/可访问性全达标）。仅剩 3 项需用户：① 真实 ICP 备案号；② 确认域名 aifunnyplay.cn；③ 上线时把广告联盟代码粘进 4 个 ad-slot。

## 20:54 续做（用户问"为啥有的照片有的没有" + 审查继续完善）
- **排查"有的有图有的没图"**：逐页/逐图扫描确认全站 81 张 `<img>` 全部能解析、0 破图；最终定位真因——`category/ai-marketing.html` 第一张卡片（SEO 文）漏了整个 `.post-featured-image` 块，只有第二张有图。这正是用户看到的"有的有有的没有"。
- **修复**：写脚本全站扫描所有 `<article class="post-card">`，对缺 `.post-featured-image` 的卡片自动补上对应文章 banner（仅补文件确实存在的图）。结果：ai-marketing 补 1 张，全站 0 张卡片缺图。临时脚本 `_fix_missing_featured.py` 已删。
- **审计 skill 再增强**：给 `audit_static_site.py` 加 `card_img` 检查（post-card 缺 featured image 即报错），并把本类问题补进 SKILL.md——以后跑审计自动抓。
- **继续完善发现第二大问题：分类数据整体对不上**。各文章自己写的"分类"与分类页所列篇数互相矛盾（如按文章元数据 ai-review 应有 13 篇但分类页只列 1 篇；ai-video-audio/ai-writing 分类页列的篇数文章元数据根本指不到）。侧边栏 `<small>(N篇)</small>` 数字也跟着错。根因：分类页是手工组的，与文章元数据早就分了家。
- **修复分类一致性**：定权威映射（20 篇按真实主题分到 6 分类，每类都有文章：ai-image4/ai-marketing2/ai-office5/ai-review1/ai-video-audio4/ai-writing4），写 `_rebuild_categories.py`：① 更新 20 篇文章的 `分类：` 元数据 + JSON-LD breadcrumb（position2 的 name/item）到规范分类；② 重建 6 个分类页的 posts-list（卡片含 featured image + 正确链接 + 摘录 + 日期 + 分类标签）；③ 全站修正侧边栏篇数。临时脚本已删。
- **复验**：侧边栏篇数偏差 0；6 分类页文章列表正确且 4/2/5/1/4/4 篇全带图；文章元数据抽查正确；破图 0；全审计 41 文件 TOTAL 0；JSON-LD 54 块 0 解析错误。
- **当前状态**：内容层面（图片一致 + 分类一致）已彻底修好。仍剩 3 项需用户拍板：① 真实 ICP 备案号；② 确认域名 aifunnyplay.cn；③ 广告联盟代码粘进 ad-slot。

## 21:32 续做（用户问"整好了吗？会有流量吗？改成有流量的"）
- **先澄清流量真相**：网站技术/SEO 已达标≠自动有流量。流量=搜索引擎收录排名+内容匹配搜索词+外链+推广+时间。前端能做的只是"流量承接能力"（内链/关键词/收录友好度），真实流量需用户提交 sitemap 到百度/Google + 知乎/小红书/公众号推广 + 持续更内容。
- **做的内链强化（SEO 最大杠杆）**：发现 20 篇相关文章推荐是随机凑的（视频文→推音乐/Logo/论文），对搜索引擎和用户都无用。写 `_rebuild_related.py`：按**同分类**重排相关推荐（同分类优先，不足补相邻分类），并升级成**带缩略图卡片**（3 列网格，提升点击率/停留时长）。结果：20 篇 × 3 卡 = 60 张 related-card，全部指向存在的 banner.svg。
- **修页脚分类 bug**：「AI编程辅助」错误指向 `category/ai-office.html`（应为 `ai-review.html`=AI检测与其他工具），涉及 20 个文件 + `tag/ai-coding.html`，已全部改对；首页 description 里"AI编程辅助"措辞改为"AI编程助手"。
- **复验**：全审计 41 文件 TOTAL 0；CSS 括号 182/182 平衡；related-card=60；破图 0；JSON-LD 54 块 0 解析错误；"AI编程辅助"残留 0；临时脚本已删。
- **交付重点**：本轮回了用户"流量获取清单"——① 备案后提交 sitemap 到百度搜索资源平台+Google Search Console；② 把 20 篇改成知乎/小红书导流文+文末留站链接；③ 每月新增 4-8 篇（重点抢"免费AI/AI工具推荐/具体工具名+教程"等长尾词）；④ 换友链/投稿拿外链。仍剩 3 项需用户：ICP 备案号、确认域名、广告代码。
